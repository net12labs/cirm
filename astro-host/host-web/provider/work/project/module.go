package project

/*

this just separates the configuration sets and configuration sets
it can point to external resources etc.
manage resources, security etc.

so we can route in 2 ways:
- root/{project_id}/{job_id}/{task_id}
- root/{job_id}/{task_id}?=project_id={project_id}

*/
